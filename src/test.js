// import React from "react";

// export const Test = ({ todos, setTodos, todo, setTodo, onClick }) => {
//   return (
//     <div>
//       <input value={todo} onChange={(e) => setTodo(e.target.value)} />
//       <button onClick={onClick}>Create</button>
//       {todos.map((t, i) => (
//         <p key={i}>{t}</p>
//       ))}
//     </div>
//   );
// };
